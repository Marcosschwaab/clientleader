<%= form_with(model: progre) do |form| %>
  <% if progre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(progre.errors.count, "error") %> prohibited this progre from being saved:</h2>

      <ul>
        <% progre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
